export interface SermonVideo {
  id: string;
  title: string;
  youtubeId: string;
}

/** Vídeos de mensagens para o carrossel — aguardando lista real do canal da IBCI. */
export const sermonVideos: SermonVideo[] = [];
